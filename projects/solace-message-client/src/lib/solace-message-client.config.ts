import { SessionProperties } from './solace.model';

/**
 * Configures the {@link SolaceMessageClient} to connect to the Solace message broker.
 */
export abstract class SolaceMessageClientConfig extends SessionProperties {
}
